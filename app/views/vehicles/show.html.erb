  <div class="card-show" style="background-image: url(<%= "#{@vehicle.image}" %>)">
    <h2><%= @vehicle.name %></h2>
    <p><%= @vehicle.city %> | <%= @vehicle.category %></p>
    <h2 class="card-trip-pricing"><%= @vehicle.price %>€</h2>
  </div>
